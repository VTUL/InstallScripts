---
- name: install building software and build essentials
  apt:
    name: '{{ item }}'
    state: present
    update_cache: yes
  with_items:
    - acl
    - build-essential
    - apt-transport-https
    - python-apt
